#include <stdio.h>
#include<iostream>
#include<fstream>
#include<vector>

void scaleExtract(unsigned int, unsigned int, int []);
int bit16toDec(unsigned int, unsigned int);
float scalingOp(int []);
int power(int, int);
void parseXyF(ifstream &, int, int, int);